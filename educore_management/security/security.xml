<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Categories -->
        <record id="module_category_educore" model="ir.module.category">
            <field name="name">EduCore Management</field>
            <field name="description">User access levels for EduCore Management System</field>
            <field name="sequence">50</field>
        </record>

        <!-- User Group -->
        <record id="group_educore_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="module_category_educore"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- Manager Group -->
        <record id="group_educore_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_educore"/>
            <field name="implied_ids" eval="[(4, ref('group_educore_user'))]"/>
        </record>

        <!-- Administrator Group -->
        <record id="group_educore_admin" model="res.groups">
            <field name="name">Administrator</field>
            <field name="category_id" ref="module_category_educore"/>
            <field name="implied_ids" eval="[(4, ref('group_educore_manager'))]"/>
        </record>

        <!-- Record Rules -->
        <record id="educore_student_rule" model="ir.rule">
            <field name="name">Student Access Rule</field>
            <field name="model_id" ref="model_educore_student"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_educore_user'))]"/>
        </record>

    </data>
</odoo>